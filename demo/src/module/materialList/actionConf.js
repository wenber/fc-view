/**
 * @file 模块 `module/materialList` - Action配置
 *
 * @author wangkemiao(wangkemiao@baidu.com)
 */
define(function (require) {
    var confList = [
        {
            path: '/module/materialList',
            type: 'module/materialList/Action'
        }
    ];

    return confList;
});
